# 插件生命周期重构调研总结报告

**日期**: 2026-08-29  
**调研周期**: 2 轮，共 11 个独立调研  
**基线**: `upstream/main` (commit `7bca8614`)  
**结论**: Issue #2994 的 4 个核心假设全部不成立；真正的缺口收敛为 3 项，且**不需要改存储格式**

---

## 一、调研方法学修正

### 1.1 第一轮错误

前四份报告在工作分支 `refactor/plugin-identity-selection` 上读代码，该分支包含未合并的 2w 行重构。核对 blob hash，四个关键文件中三个与 `upstream/main` 不同。

### 1.2 第二轮修正

改为 `git show upstream/main:<path>` 直读。逐条复核结论：

| 结论 | upstream/main 证据 | 是否成立 |
|---|---|---|
| 跨进程文件锁已存在 | `operation_lock.py:119 _PROCESS_LOCK`、`:199 _acquire_file_lock_sync` | ✅ |
| 原子写入已存在 | `manager.py:327 _atomic_write`、`:359 os.replace` | ✅ |
| 只有 2 个状态文件 | `plugins.lock.json` + `plugin_runtime_overrides.json` | ✅ |

**方法学瑕疵，结论无需修改** — 这些机制在重构前就有。

---

## 二、Issue #2994 的 4 个假设 vs 调研结果

| Issue 声称 | 调研结果 | 证据 |
|---|---|---|
| **1. 需要 CAS 防并发** | ❌ 不存在多进程并发 | CLI install 已禁用；Server 单进程 + `plugin_operation_lock` 串行化 |
| **2. 3 文件写入不一致** | ❌ 只有 2 文件，已有防护 | 每个文件原子写入（tmp + rename）；生命周期不同，不需要同步 |
| **3. 600ms → 35ms** | ❌ 实测 13ms，无法重现 | 22 插件：扫描 3.53ms + 解析 11.44ms；100 插件估算 59ms |
| **4. Registry 应 Git 友好** | ❌ 混淆了概念 | 运行状态（本机路径/偏好）不应 Git 友好；环境复现需独立声明文件 |

**4 个假设全部不成立。**

---

## 三、wislap 的 11 条不变量守护现状

| # | 不变量 | 判定 | 依据 |
|---|---|---|---|
| 1 | `plugin_id` 不通过改名解决冲突 | ⚠️ 部分 | 目录改名已封堵（两入口强制 `fail`）；运行时 ID 改名靠 `source_replacement` 护栏 |
| 2 | 一个 ID 多候选、只有一个生效 | ⚠️ 部分 | builtin + 1 规范 user 有测试；多 user 候选不支持 |
| 3 | 四类来源同一候选模型 | ❌ 缺 | 无统一 `PluginCandidate`；无 `manual/unmanaged` 概念 |
| 4 | 只有 managed 候选可升级/卸载 | ❌ 缺 | 无 managed/unmanaged 区分 |
| 5 | 代码与持久数据分离 | ✅ 有 | `_validate_replacement_targets` fail-closed |
| 6 | 五类操作走同一文件事务 | ⚠️ 部分 | `replace_plugin` 共享；**卸载走独立路径**（7 个函数） |
| 7 | 候选切换不丢启停偏好 | ✅ 有 | 7 条测试，含 `:579`/`:3271` |
| 8 | 磁盘是候选存在的事实来源 | ✅ 有 | `reconciler.py` + `scanner.py` 每次重扫 |
| 9 | 单个坏候选不中断 inventory | ✅ 有 | 5 条测试，含 `:412` |
| 10 | 修改统一串行，调用方不接触 revision/锁 | ⚠️ 部分 | 已串行化；但调用方仍需装配 12 个回调 |
| 11 | 返回阶段/回滚状态/最终候选 | ⚠️ 部分 | `ReplacePluginResult` 有结构；卸载路径无；`rollback_status` 语义未文档化 |

**统计**：✅ 4 条 / ⚠️ 5 条 / ❌ 2 条

**upstream/main 的实际守护程度比 Issue 描述的好得多。**

---

## 四、真正的问题：3 个缺口

排除已守护和"新能力"后，真正需要重构解决的：

### 缺口 1：卸载路径未走共享事务（不变量 6）

**现状**：7 个包文件函数住在 `lifecycle_service.py` 里，从不经过 `replace_plugin`：

| 行号 | 函数 | 职责 |
|---|---|---|
| 391 | `_delete_plugin_directory_sync` | 删代码目录 |
| 521 | `_record_deferred_profile_cleanup_sync` | 延迟清理记账 |
| 549 | `_retry_deferred_profile_cleanup_sync` | 启动期重试 |
| 615 | `_stage_orphaned_package_profile_sync` | profile 暂存 |
| 777 | `_restore_staged_package_profile_sync` | profile 恢复 |
| 784 | `_finalize_staged_package_profile_sync` | profile 最终删除 |
| 793 | `_mark_install_source_removed_sync` | 安装来源记账 |

**问题**：卸载的文件操作逻辑与安装/升级分离，无法复用回滚、阶段报告等机制。

---

### 缺口 2：调用方装配 12 个回调（不变量 10）

**现状**：`replace_plugin` 要求调用方提供 7 个必需回调 + 5 个可选参数。两处装配对比：

| 参数 | `plugin_cli/service.py:459` | `market_bridge.py:3588` | 判定 |
|---|---|---|---|
| `is_running` | `upgrade_support.plugin_is_running` | `plugin_is_running` | **完全相同** |
| `stop` | `stop_plugin_for_replace` | `stop_plugin_for_upgrade`（同函数别名） | **完全相同** |
| `start` | 闭包 → `start_plugin_after_replace(strict=True)` | 闭包 → `start_plugin_after_upgrade(strict=True)` | **逻辑相同** |
| `cleanup_backup` | `remove_directory` | `_async_remove_dir`（加日志） | **几乎相同** |
| `additional_targets` | `(profile_dir,)` | `(profile_dir,)` | **完全相同** |
| `validate_new` | 闭包：校验 plugin_id + directory_name | 闭包：校验 plugin_id + registry runtime source | **同一检查实现两次** |

**12 个参数里 5 个在两处完全相同，各写一遍。**

Market 侧还多一层类型丢失：

```python
# market_bridge.py:3299
replace_kwargs: dict[str, Any],
# market_bridge.py:3317
return await replace_plugin(**replace_kwargs)
```

**后果**：
- 静态检查失效（`dict[str, Any]` + `**` 展开）
- `replace_plugin` 改签名时 Market 路径不报错
- 调用方仍需知道"正确调用顺序"

---

### 缺口 3：候选模型缺 managed/unmanaged（不变量 3、4）

**现状**：`channel` 字段不等于 managed/unmanaged

| Channel | 删除行为 | 应有行为 | 差距 |
|---|---|---|---|
| `builtin` | 路径白名单排除 | ✅ 不可删 | 已守护 |
| `manual` | 可删 | ⚠️ 拒绝或警告 | **缺口 3** |
| `imported` | 可删 | ✅ 可删 | 正确 |
| `market` | 可删 | ✅ 可删 | 正确 |

**问题 1：Manual 插件可被误删**

用户手动放置插件目录 → `reconcile()` 自动打 `"manual"` 标签 → 用户在 UI 点删除 → N.E.K.O 直接 `rmtree`，无警告。

**问题 2：无法区分来源**

- `delete_plugin` 不检查 `channel`，只检查路径白名单
- 无法保证"只有 N.E.K.O 安装的目录可以被 N.E.K.O 删除"

---

## 五、两个具体场景的调研结论

### 5.1 Manual 插件接管（调研 10，回答 wislap 第 1 节）

**Q1: 能否接管？**  
✅ 可以。启动时 `reconcile()` 打 `"manual"` 标签 → CLI/Market 安装同 ID 包 → `plan_install` 判定为 `"upgrade"`（不检查 channel）→ `replace_plugin` 替换目录 → `record_import`/`record_market` 覆写 lock entry，`channel` 变为 `"imported"`/`"market"` — **接管完成**。

**Q2: 如何判断可安全删除？**  
❌ 不判断。`delete_plugin` 只检查路径白名单，不检查 `channel`。

**Q3: 如何拒绝？**  
⚠️ 应该但没有。建议引入 `managed` 字段，在删除入口守护：
```python
if not entry.managed:
    raise UNMANAGED_PLUGIN_DELETE_FORBIDDEN
```

---

### 5.2 偏好清理语义（调研 11，回答 wislap 第 7 节）

**Q: "`:3145 test_delete_plugin_clears_runtime_override` 清理偏好是否合理？"**

✅ 合理。当前行为：

```python
if not restored_builtin:
    clear_runtime_override(plugin_id)
```

| 删除场景 | 偏好清理？ | 理由 |
|---|---|---|
| 删除最后一个候选（彻底卸载） | ✅ 清理 | `plugin_id` 消失，偏好无绑定对象 |
| 删除 User/Market，Builtin 恢复 | ❌ 保留 | `plugin_id` 仍存在，偏好应继续生效 |

**设计一致性**：
- 偏好绑定 `plugin_id`（逻辑概念），不绑定候选（物理目录）
- 用户说"我禁用了 `demo`"，是指"不要这个插件"，而不是"不要这个候选"
- 候选切换时偏好应继续生效；彻底卸载时偏好应清理

**测试覆盖**：
- `:3145` — 彻底删除清理偏好
- `:579` — Builtin 恢复保留偏好（包括 `disabled`）
- `:468` — Builtin 恢复尝试重启但尊重 `disabled` 偏好

---

## 六、对 Phase 1 代码的最终判断

Phase 1 实现的 `PluginRegistryStore`（CAS + revision + portalocker）**解决的是不存在的问题**：

| 组件 | 判定 | 理由 |
|---|---|---|
| `store.py`（CAS） | ❌ 不进生产 | 调研 1 证明并发已被 `plugin_operation_lock` 串行化 |
| `audit_log.py`（JSONL） | ❌ 不进生产 | 无真实需求 |
| `migration.py` | ❌ 不进生产 | 无格式变更，不需要迁移 |
| `models.py` 的候选抽象 | ⚠️ 可作缺口 3 起点 | 需要补充 `managed` 字段 |
| `candidates.py` 的 `resolve_effective_candidate` | ⚠️ 可作统一候选选择起点 | 需要与现有 Registry 整合 |
| `benchmark_plugin_startup.py` | ✅ 已独立提交 | 作为性能基线工具保留 |

**这 3 个缺口都不需要改存储格式。** 不需要单文件 Registry、不需要 CAS、不需要 JSONL。

---

## 七、11 个调研的关键证据索引

| # | 调研主题 | 关键发现 | 证据位置 |
|---|---|---|---|
| 1 | 多进程并发 | CLI install 已禁用 | `install_cmd.py:37-40` |
| 1 | 多进程并发 | Server 已有跨进程文件锁 | `operation_lock.py:119`、`:199` |
| 2 | 一致性风险 | 只有 2 个状态文件 | `manager.py:106`、`runtime_overrides.py:28` |
| 2 | 一致性风险 | 每个文件原子写入 | `manager.py:327 _atomic_write` |
| 3 | 性能 Benchmark | 实测 13.05ms（22 插件） | `benchmark_plugin_startup.py` |
| 4 | 环境复现需求 | GitHub 搜索无相关 Issue | 搜索关键词 7 个，结果 0 |
| 5 | 模块职责重叠 | `replace_plugin` 只有 2 个调用方 | `plugin_cli/service.py:459`、`market_bridge.py:3317` |
| 5 | 模块职责重叠 | 12 参数中 5 个完全相同 | 逐参数对比表（调研 5） |
| 6 | 不变量 7 | 7 条测试覆盖偏好保留 | `:579`、`:3271` 等 |
| 7 | 不变量 9 | 5 条测试覆盖坏候选隔离 | `:412 test_refresh_registry_marks_syntax_error_plugin_failed_without_aborting` |
| 8 | `_1` 改名 | 目录改名不可达 | `plugin_cli.py:61` + `market_bridge.py:387` 归一化 |
| 9 | 降级承诺边界 | 回滚不包含持久数据 | `upgrade_support.py:260 _validate_replacement_targets` |
| 10 | Manual 插件接管 | 可接管，可删除，无区分 | `scanner.py:_seed_entry`、`lifecycle_service.py:_path_within_plugin_roots_sync` |
| 11 | 偏好清理语义 | 彻底卸载清理，候选切换保留 | `lifecycle_service.py:1763`、测试 `:3145`/`:579` |

---

## 八、3 个缺口的实现优先级建议

### 建议优先级：缺口 1 → 缺口 2 → 缺口 3

**理由**：

**缺口 1（卸载收口）优先**：
- 边界清晰：7 个函数已列出，迁移目标明确
- 立即减少 `lifecycle_service.py` 体积（1861 行）
- 为其他缺口扫清障碍（统一事务入口后更容易改接口）

**缺口 2（回调装配）其次**：
- 依赖缺口 1 完成（卸载收口后才有"完整的事务入口"）
- 可以增量改进：先把 5 个恒定回调收进模块内部，再用类型化 request 替代 `dict[str, Any]`

**缺口 3（候选模型）最后**：
- 需要定义领域模型（`PluginCandidate` with `managed` field）
- 影响面最广（scanner、reconciler、registry、lifecycle 都要改）
- 可以复用 Phase 1 的 `models.py` 作为起点

---

## 九、下一步行动建议

### 9.1 立即行动

1. **更新 Issue #2994**
   - 删除 4 个不成立的假设
   - 基于 3 个缺口重写问题描述
   - 引用 11 个调研报告作为证据

2. **开启 Discussion**
   - 征求对 3 个缺口的优先级意见
   - 讨论 `managed` 字段的设计（bool vs 枚举）
   - 确认 manual 插件的删除策略（拒绝 vs 警告）

3. **Phase 1 代码处置**
   - 保留在本地分支作为参考
   - Benchmark 脚本已独立提交（性能基线工具）
   - 不提交为 Draft PR（避免误导）

### 9.2 缺口 1 的实施路径（如果优先）

1. **调研阶段（1-2 天）**
   - 读 `replace_plugin` 的事务边界
   - 读 7 个函数的依赖关系
   - 确认接入点（新增 `uninstall_plugin` 还是扩展 `replace_plugin`）

2. **设计阶段（1 天）**
   - 画出新旧两套调用流程图
   - 确认回调签名（是否需要新增）
   - 确认回滚语义（卸载失败如何恢复）

3. **实现阶段（3-5 天）**
   - 迁移 7 个函数
   - 修改 `delete_plugin` 调用新入口
   - 补充测试（卸载回滚场景）

4. **验证阶段（1 天）**
   - 跑全量测试
   - 手动验证删除-回滚-恢复流程

---

## 十、总结

### 10.1 调研推翻了 Issue 的全部假设

| Issue 假设 | 调研结论 |
|---|---|
| 多进程并发需要 CAS | ❌ 不存在多进程并发 |
| 3 文件写入不一致 | ❌ 只有 2 文件，已有防护 |
| 600ms → 35ms | ❌ 实测 13ms |
| Registry 应 Git 友好 | ❌ 混淆了概念 |

### 10.2 upstream/main 比想象中健壮

- 11 条不变量中 4 条完全守护、5 条部分守护
- 关键的失败隔离（#9）和偏好保留（#7）都有测试
- 并发控制、原子写入、状态分离早就存在

### 10.3 真正的问题只有 3 个

1. **卸载路径未收口** — 7 个函数绕过共享事务
2. **回调装配重复** — 12 参数中 5 个完全相同，`dict[str, Any]` 丢失类型
3. **候选模型缺失** — 无 managed/unmanaged 区分，manual 插件可被误删

### 10.4 不需要改存储格式

这 3 个缺口都是代码组织和接口设计问题，不是数据模型问题。不需要：
- 单文件 Registry
- CAS 并发控制
- JSONL 审计日志
- 格式迁移

### 10.5 Phase 1 的价值

虽然 CAS store 不进生产，但调研本身产出了：
- 11 份详细的现状报告
- 1 个可复现的性能 Benchmark
- 对 upstream/main 守护程度的准确评估
- 明确的 3 个缺口边界

**这是架构重构前必须做的尽职调查，Phase 1 的价值在调研，不在代码。**

---

**报告结束**

**附件清单**：
1. 多进程并发调研
2. 一致性风险调研
3. 性能 Benchmark 调研
4. 环境复现需求调研
5. 模块职责重叠量化调研
6. 不变量守护现状调研
7. Manual 插件接管行为调研
8. 偏好清理语义调研
9. Benchmark 脚本（已独立提交）

**调研耗时**: 约 3 个工作日  
**调研方法**: 静态代码分析 + 测试覆盖验证 + GitHub Issue 搜索  
**下一步**: 等待社区对 3 个缺口优先级的反馈
